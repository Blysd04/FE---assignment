import React from 'react';
// import PropTypes from 'prop-types';

NotFound.propTypes = {

};

function NotFound(props) {
  return (
    <div style={{ color: "#D30000", display: "flex", justifyContent: "center", alignItems: "center" }}>
      NOT FOUND
    </div>
  );
}

export default NotFound;